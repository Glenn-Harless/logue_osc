# Agent Contract (bd-first)

We track work in Beads instead of Markdown. Run `bd quickstart` to see how.

All task planning and execution MUST use Beads (bd). Do not write ad-hoc Markdown plans or TODOs.